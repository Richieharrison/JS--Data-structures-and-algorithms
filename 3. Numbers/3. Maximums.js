// Maximums

const bool = Number.MAX_VALUE + 1.1 === Number.MAX_VALUE + 2.022;

// Minimums

const result = Number.MIN_VALUE // returns the smallest floating point number

//Infinity - the only thing greater than Number.MAX and the only smaller than Number.MAX_SAFE_INTEGER


console.log(result);