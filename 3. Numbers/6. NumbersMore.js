// Math Random

const rand = Math.random(); // returns a float between 0 and 1
const rand100 = Math.random() * 100; // returns a float btwn 0 and 100
const randMore = Math.random() * 25 + 5; // returns a float btwn 5 and 30
const randTry = Math.random() * 10 - 100; 
const randFloor = Math.floor(Math.random() * 100); // integers between 0 and 99